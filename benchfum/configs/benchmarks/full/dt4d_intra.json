{
  "_name": "DT4D_r intra-class benchmark",
  "_description": "Full-shape matching on DT4D remeshed using only same-category pairs.",

  "dataset": {
    "type": "DT4DPairsDataset",
    "root": null,
    "_note": "Intra-class mode: only pairs from the same category.",
    "phase": "test",
    "inter_class": false,
    "spectral": true,
    "k": 200,
    "_ignored_categories_default": ["pumpkinhulk"]
  },

  "n_pairs": 1000,

  "metrics": ["euclidean_error"],

  "methods": [
    {
      "name": "WKS",
      "matcher_config": "../../matchers/features_only/wks.json"
    },
    {
      "name": "FMap+WKS",
      "matcher_config": "../../matchers/functional_maps/fmap_wks.json"
    }
  ]
}
